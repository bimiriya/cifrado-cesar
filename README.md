
```javascript
    crear función caesarCyph()
	hacer do 
	if (frase = 1) 
	llamar función cipher()
	else if (frase = 2)
	llamar función decipher()
	while (frase prompt !== '' && frase prompt !== 1 o 2)


crear función cipher()
    frase = prompt.touppercase
	hacer do 
	if (frase prompt hace .match con a-z) 
	hacer for i < frase prompt.length
	frase prompt.charcodeat(i) = posición
	posición = string . fromcharcode - 65 +33 % 26 +65
	empty string = posición
	else 
	alert('ingresar texto válido')
	return document.write(emptystring)
	while (typeof frase !== string)

crear función decipher()
    frase = prompt.touppercase
    hacer do
    if (frase prompt hace match con a-z)
    hacer for j < frase prompt.length
    frase prompt.charcodeat(j) = posición
    posición = string . fromcharcode -65 -7 %26 +65
    empty string = posición
    else 
    alert('ingresar texto válido')
    return document.write(empty string)
    while (typeof frase !== string)

    llamar función caesarCyph()

    ```

    DIAGRAMA DE FLUJO : https://www.lucidchart.com/documents/view/0baf4233-e4a7-4310-bdf8-0b573fd36af2